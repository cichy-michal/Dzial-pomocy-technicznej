<%= form_with(model: ticket) do |form| %>
  <% if ticket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
        <% ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <% if defined? editing %>
    <%= form.label :Dodaj_rozwiazanie, style: "display: block; color: white;" %>
    <% else %>
    <%= form.label :Opis, style: "display: block; color: white;" %>
    <% end %>

    <%= form.text_area :description, rows: 20, cols: 100 , style: "background-color: #343a40;color: white;"%>
  </div>

  <div>
<%= form.label :"Dodaj zdjęcie", style: "display: block; color: white;" %>
    <%= form.file_field :image , style: "background-color: #343a40;color: white;", accept: 'image/png, image/gif, image/jpeg'%>
  </div>

  <div>
    <%= form.text_field :status, value: 'Nowy', type: :hidden %>
  </div>

  <div>
    <%= form.number_field :employee_id, value:@employee.id, type: :hidden %>
  </div>
<hr style="margin-top: 10px; margin-bottom: 1px;" />
  <div>
    <%= form.submit "Aktualizuj zgłoszenie"  , class: "btn btn-dark"%>
     
  </div>
  
<% end %>
